<nav class="navbar sticky-top bg-light">
    <div class="container justify-content-start">
        <% pills.forEach(pill=> { %><a class="badge text-bg-secondary me-1 mb-1" href="<%- pill.link %>"><%- pill.name %></a><% }); %>
    </div>
</nav>

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">name</th>
            <th scope="col">size</th>
        </tr>
    </thead>
    <tbody>
        <% items.forEach(item=> { %>
        <tr>
            <td>
                <a class="<% if (item.isDir) { %>text-dark<% } %>" href="<%- item.parent === ''
                    ? encodeURIComponent(item.name)
                    : item.parent.split(/[\\/]/g).concat(item.name).map(p => encodeURIComponent(p)).join('/')
                %>">
                <span class="text-warning"><% if (item.isDir) { %><%- include(`../icons/folder.svg`); %><% } %></span>

                    <%=item.name%>
                </a>
            </td>
            <td><%= item.size %></td>
        </tr>
        <% }); %>
    </tbody>
</table>
